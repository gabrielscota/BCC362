name: tp2_server
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ^3.5.1

dependencies:
  googleapis: ^13.2.0
  googleapis_auth: ^1.6.0
  http: ^1.2.2
  logging: ^1.2.0
  redis: ^4.0.0
  retry: ^3.1.2
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
